{
  "name": "backend-shared",
  "projectType": "library",
  "root": "packages/backend/shared",
  "sourceRoot": "packages/backend/shared/src",
  "targets": {
    "lint": {
      "executor": "nx:run-commands",
      "options": {
        "command": "biome check --diagnostic-level=warn packages/backend/shared"
      }
    },
    "test": {
      "executor": "@nx/vitest:test",
      "outputs": ["{projectRoot}/coverage"],
      "options": {
        "configFile": "packages/backend/shared/vitest.config.ts"
      }
    }
  }
}
