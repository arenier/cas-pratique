{
  "name": "backend-kernel",
  "projectType": "library",
  "root": "packages/backend/kernel",
  "sourceRoot": "packages/backend/kernel/src",
  "targets": {
    "test": {
      "executor": "@nx/vitest:test",
      "outputs": ["{projectRoot}/coverage"],
      "options": {
        "configFile": "packages/backend/kernel/vitest.config.ts"
      }
    },
    "lint": {
      "executor": "nx:run-commands",
      "options": {
        "command": "biome check --diagnostic-level=warn packages/backend/kernel"
      }
    }
  }
}
