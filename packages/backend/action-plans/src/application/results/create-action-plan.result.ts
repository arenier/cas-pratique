/**
 * Result returned after creating an action plan.
 */
export type CreateActionPlanResult = {
  /** Action plan identifier. */
  actionPlanId: string;
  /** Organization identifier. */
  organizationId: string;
};
